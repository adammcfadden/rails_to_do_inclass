<h1 class="page-header text-center">New List</h1>

<%= render "form" %>

<!-- <% if @list.errors.any? %>
  <h3>You done messed up.</h3>
  <ul>
    <% @list.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<div class="form-group">
<%= form_for @list do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.label :description %>
  <%= form.text_area :description, size: "50X50" %>
  <%= form.submit %>
<% end %>
</div> -->
